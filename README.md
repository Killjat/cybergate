# ⚡ CyberGate

**情报行动的数字身份基础设施。**

---

## 背景

2024年，全球超过60%的有价值情报藏在登录墙后面。

社交媒体的私密群组、论坛的内部讨论、搜索引擎的个性化结果、职业网络的人脉关系 —— 这些内容对匿名访问者永远关闭。传统爬虫在这里失效，API 在这里受限，只有**真实的、有历史的、经过验证的数字身份**才能穿透这道墙。

CyberGate 就是为此而生。

---

## 想象这样一场情报行动

**目标**：追踪某个组织在多个平台上的动态，分析其成员构成、资金流向、舆论策略。

**传统做法**：
- 手动登录各平台，逐一查看
- 账号被封，重新注册，重新登录
- 信息分散，无法形成系统性情报

**用 CyberGate 的做法**：

```
第一步：在指挥机器上完成所有账号的登录
        ↓
        10个 Google 账号，10个 Reddit 账号，5个 LinkedIn 账号
        每个账号都有独立的浏览器身份，有历史记录，有真实行为特征
        ↓
第二步：将这些身份打包，分发到全球不同节点的服务器
        ↓
        纽约的服务器用账号A监控英文社区
        新加坡的服务器用账号B追踪亚太动态
        法兰克福的服务器用账号C分析欧洲舆论
        ↓
第三步：各节点的 AI Agent 以真实用户身份并发工作
        ↓
        情报汇聚，交叉验证，形成完整画像
```

这不是爬虫，这是**数字卧底网络**。

---

## 核心能力

### 身份即资产

每一个账号在 CyberGate 里不只是用户名和密码，而是一个完整的**数字身份**：

- 独立的 Chrome Profile，包含完整的 Cookies、Session、浏览历史
- 登录一次，身份永久有效，可在任意机器上复活
- 打包传输，部署到全球任意节点，30秒内激活

```bash
# 将一个已登录的身份部署到远程服务器
scp identity_google_account1.tar.gz agent-server-tokyo:/opt/identities/
# 远程 Agent 打开浏览器，直接就是登录状态
```

### 反检测登录引擎

Google、Reddit 的风控系统每天拦截数百万次机器人登录。CyberGate 的登录流程让它们无从识别：

- 从空白地址栏开始，手动输入目标网址
- 在主页随机停留、滚动、浏览，模拟真实用户节奏
- 逐字符输入，带随机停顿，符合人类打字特征
- 鼠标自然移动，悬停后点击，无直线轨迹
- 全程真实 Chrome，无 WebDriver 特征，无自动化标记

对平台的风控系统来说，这就是一个普通用户在登录。

### 分布式身份调度

```
                    ┌──────────────────────────┐
                    │       CyberGate          │
                    │   身份管理 · 登录 · 分发  │
                    └─────────────┬────────────┘
                                  │
           ┌──────────────────────┼──────────────────────┐
           ↓                      ↓                      ↓
     节点 A / 北美           节点 B / 欧洲           节点 C / 亚太
   [身份1] [身份2]          [身份3] [身份4]         [身份5] [身份6]
        ↓                        ↓                       ↓
   监控英文社区              追踪欧洲舆论             分析亚太动态
```

每个节点的 Agent 拿到的是一个活的数字身份，不是账号密码。打开即用，无需任何额外配置。

### API 优先，无缝集成

情报系统直接调用，全程自动化：

```python
# 触发自动登录
POST /api/start-auto-login/{account_id}

# 用已有 Session 打开浏览器，Agent 接管
POST /api/open-browser/{account_id}
# 返回: { "instance_id": "...", "tab_id": "..." }

# 查询登录状态
GET /api/login-status/{task_id}
```

---

## 典型场景

**竞争情报**
监控竞争对手在 LinkedIn、Reddit、Twitter 上的招聘动态、产品讨论、用户反馈。多账号轮换，避免被识别为爬虫。

**舆情监控**
追踪特定话题在不同平台、不同地区的传播路径。每个节点用本地 IP + 本地账号，获取真实的地域化内容。

**OSINT 调查**
以真实用户身份访问目标的社交媒体、公开资料、关联账号，构建完整的人物或组织画像。

**市场研究**
用不同地区的账号访问同一平台，获取差异化的本地内容和价格信息，发现市场机会。

---

## 快速开始

```bash
# 启动 PinchTab（浏览器控制层）
pinchtab server

# 启动后端
cd backend && python main.py

# 启动前端
cd frontend && npm start
```

打开 `http://localhost:3000`，注册第一个账号自动成为管理员。

添加账号 → 点击自动登录 → 身份固化 → 分发部署 → Agent 接管。

---

## 安全

- 密码 Fernet 加密存储，密钥本地保管
- JWT 认证，角色权限隔离
- PinchTab 默认只监听本地，不对外暴露
- Profile 目录安全级别等同于密码，请加密存储和传输

---

*CyberGate — 每一个 AI Agent，都应该有自己的数字身份。*
